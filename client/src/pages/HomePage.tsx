import React from "react";
import Courses from "../components/Courses";

const Homepage = () => {
    return (
        <div>
            <Courses></Courses>
        </div>
    );
};

export default Homepage;